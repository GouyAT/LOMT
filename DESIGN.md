# 诡秘剧场 · 前端原型 5「暗房样张」设计计划

> The Contact Sheet —— **合成体**：P3 的暗房视觉 + P4 的信息架构 + P1 的控件克制度。
> 本文是先于代码的设计计划；所有颜色 / 字体 / 布局 / 控件数量都从这里派生。

---

## 0. 为什么要有第五代

`prototype-comparison.md` 的实测结论：**P3 与 P4 把过半首屏控件预算花在导航上**
（P3 底片条等 49/72 = 68%；P4 五条并行通道 29/57 = 51%），移动端 `<44px` 触控目标 19—23 个。
病根不是"功能做多了"，而是**把功能清单直接翻译成入口矩阵**。

P5 的立论只有一句：**砍入口，不砍能力。**
24 个面板一个都不减，但通向它们的常驻通道从 2—5 条压到 **1 条**。

| | P3 灵异显影室 | P4 汞镜案台 | **P5 暗房样张** |
|---|---|---|---|
| 面板数 | 24 | 22 | **24（不减）** |
| 面板入口通道 | 2（底片条 + 轨道） | 5（星图/左轨/右轨/速取/dock） | **1（样张）** |
| PC 首屏 chrome 控件 | 70—72 | 54—57 | **15（最坏 19）** |
| 移动端首屏 chrome 控件 | 34 | 28 | **8（最坏 11）** |
| 移动端 `<44px` 触控目标 | 22 | 23 | **0** |

---

## 1. 核心装置：把底片条改造成接触印相样张

P3 把 24 个面板做成常驻底片条钉在屏幕底部——那等于把索引**永久展开**。
但在真实暗房里，一整卷底片的索引本来就有个专门装置：

> **接触印相样张（contact sheet）**——把底片直接压在相纸上曝一次光，
> 得到一整张缩略图，用来挑**哪一格值得放进放大机**。

于是 P5 的信息架构与暗房语汇天然对齐：

| 交互 | 暗房动作 |
|---|---|
| 调出面板索引 | 取来样张（`` ` `` / `Ctrl+K` / 顶梁一个钮 / 移动端底部一格） |
| 浏览 24 个面板 | 一张纸上 6×4 格，每格一格底片，按段落染底色 |
| 打开一个面板 | 把那一格**架进放大机**，投影到相纸上 |
| 已打开的面板 | 样张上那一格转成**定影琥珀**边框 |
| 收起面板 | 收回**工作台的底片夹** |

这比 P4 的星座启动器更好的三点：**天然网格**（不必设计 24 个坐标）、
**自带分段**（三段染底色即可，不需要额外图例控件）、
**动作即隐喻**（"挑一格去放大"就是"打开一个面板"本身）。

---

## 2. 设计令牌（承袭 P3，值不改）

沿用 P3 `styles/tokens.css` 的全部色值与字体角色，保证 P5 读起来是 P3 的血脉：

```
暗室 60%   --void #100c0d  --room-1 #171113  --room-2 #211719  --room-3 #2c2022
相纸 30%   --paper #f1eade --paper-2 #e0d6c4 --ink #23201c     --ink-2 #514a41
银盐       --silver #c4ccd1 --silver-2 #8f989e --silver-4 #3d4448
强调 10%   --safelight #d2452e（安全灯红：氛围与危险）
           --cyan #2a6d90    （蓝晒蓝：信息与结构）
           --fixer #c9913c   （定影琥珀：收益，克制）
```

**纪律：红管氛围与危险，蓝管信息与结构，银管金属与分隔——三者永不在同一元素上混用。**

字体四角色同样承袭：**Bodoni Moda**（Didone 铭牌/英文题头）· **Noto Serif SC**（中文正文与标题）·
**Courier Prime**（编号/坐标/token/JSON）· **楷体**（相纸背面手写批注，极克制）。

P5 新增的令牌只有布局与层级：`--tap-min: 44px`（移动端触控地板）、`--bench-h`、`--z-contact`、`--z-shutter`。
**注意：没有 `--rail-w`** —— 因为 P5 没有侧栏图标轨道。

---

## 3. 控件预算（这是 P5 的第一性约束）

### PC 首屏常驻 chrome：19 个（上限 22）

| 区域 | 控件 | 数量 |
|---|---|---|
| 暗房横梁 | 样张 / 端切换 ×2 / 皮肤 | 4 |
| 显影台工具条 | 三态**循环**钮（一个钮走完三态） | 1 |
| 晒片绳 | 行动相片 ×4 | 4 |
| 输入区 | 输入框 / 快门键 / 回退 / 回合菜单 | 4 |
| 显影槽（左） | 脊标（折叠整栏） | 1 |
| 相片墙（右） | 脊标 / 今日头条 / 在场 ×3 | 5 |
| 工作台（底） | **0**（只放已收回的底片夹与只读读数） | 0 |

### 移动端首屏常驻 chrome：11 个（上限 12）

切回桌面端 1 · 三态循环 1 · 行动相片 4 · 输入 1 · 快门 1 · 底部三标签 3。
**移动端额外减法**：样张钮走底部标签、皮肤钮走设置面板、当前端形态钮冗余移除、回退并入回合菜单。

### 被砍掉的东西（对照 P3/P4）

1. **侧栏图标轨道**（P4 左 6 + 右 9）→ 删。侧栏只放内容，不放入口。
2. **卷宗速取矩阵**（P4 8 张卡）→ 删。速取就是样张。
3. **常驻指令 chip 行**（P4 9 个）→ 改为输入框内打 `/` 的自动完成（借聊天肌肉记忆，0 常驻控件）。
4. **三态三个钮** → 1 个循环钮（`1/2/3` 快捷键保留）。
5. **回合操作 5 钮**（重演/变量重算/存档/中断）→ 收进 1 个「回合菜单」。
6. **左栏 4 个折叠头** → 全改只读读数区（`.readout`），一眼看完，不给每项一个按钮。
7. **常驻 24 格底片条**（P3）→ 按需调出的样张。
8. **工作台不重复列出已打开的窗口** → 只列已收回的，避免入口二次冗余。

### 闸门

`checks.mjs` 内置**控件预算断言**：PC ≤22、移动端 ≤12、`<44px` 触控目标 =0、面板通道 =1。
并且额外测**最坏情形**（把相纸滚到底、4 张行动相片同时可见）。超预算即 FAIL。

---

## 4. Diegetic 体系：界面即设定

P5 把 Diegetic 推进一层——**界面不只是"像"暗房，故事就发生在暗房里**。
第七回合，以利亚在《廷根晚报》地下暗房冲洗昨夜在翡翠剧院拍的干版；第三块板子上有两个影子。

| 界面元素 | 暗房实物 |
|---|---|
| 页面底 | 暗室，只有安全灯的红（唯一光源，缓慢呼吸） |
| 顶梁 | 安全灯 + 管线四灯 + 曝光计（**全是只读指示物，不是控件**） |
| 六维属性 | **显影量筒**（玻璃管 + 药液 + 弯月液面 + 刻度 + 高光扫过） |
| 正文 | **相纸**（银盐逐字显影：blur → clear，边缘显影不均 + 定影泛黄边） |
| 行动选项 | **晒片绳上的相片**（木夹 + 悬停摆动 + 按下沉） |
| 面板索引 | **接触印相样张**（24 格，按段落染底色） |
| 面板 | **放大机投影**（打开时先过曝再收敛；可拖拽/并排/收回/钉住） |
| 通知 | **相纸小样**（从雾里显影 + 倒数条 + **整体点击穿透**，只有关闭钮可交互） |
| 确认框 | **显影计时器**（倒数转盘落下 + Esc/Enter） |
| 警告 | **漏光**（安全灯抖动 + 四边漏光，环境级反馈，不弹红框） |
| 探索热区 | **放大镜圈** |
| 开场 | 空白相纸在**显影盘**里浮出标题 → **快门**闭合 → 静止换台 → 张开 |
| 读心 | 相纸背面的**手写批注**（楷体） |
| 污染/失控 | **过曝** |
| 角色头像 | **玻璃干版肖像**（划痕 + 反光扫过） |

---

## 5. 24 格底片（面板清单）

**叙事段 N°01—06**（底色：安全灯红调）
王国舆图 · 图鉴 · 编年史 · 占卜间 · 廷根晚报 · 人物关系

**玩法段 N°07—15**（底色：蓝晒蓝调）
角色卷宗 · 序列与能力 · 行囊与装备 · 伏笔台账 · 战术棋盘 · 侦探本 · 领地经营 · **剧场活动** · 一键生图

**系统段 N°16—24**（底色：中性室灰）
档案馆 · 世界书 · 上下文合成器 · API 管线 · 变量编辑器 · **记忆精炼器** · 记忆召回 · 信札 · 设置

> 加粗两格是 P5 相对 P4 新增：**剧场活动**（剧场点数买叙事特权，代价是人性）与
> **记忆精炼器**（小总结→大总结→二次精炼三级 + 别名洗白）。

---

## 6. 微交互（有目的，不为动效而动效）

| 对象 | 交互 |
|---|---|
| 按钮 | hover：安全灯掠光横掠 + 上浮 1px；active：下沉 + 显影涟漪（从光标点扩散） |
| 图标 | hover：线条描边重绘（stroke-dashoffset） |
| 量筒 | 数值变化：液面弹性升降 + 高光扫过 + 刻度微亮 |
| 相纸正文 | 逐字显影 blur(3.4px)→0 + 字距收拢（点击可跳过） |
| 晒片绳相片 | 逐张挂上（超冲回摆）；hover 微微摆动，奇偶反向 |
| 样张 | 打开时 24 格**逐格印出**（过曝→收敛）；格子 hover 抬起放大 |
| 放大机窗口 | 打开：从样张那一格 FLIP 飞入 + 先过曝再收敛；聚焦窗口显示光锥 |
| 底片格头像 | 负像，hover 反转成正像 |
| 输入行 | 聚焦：安全灯线从中心向两端延展 |
| 开场 | 相纸落入显影盘 → 标题逐字显影 → 快门五叶闭合/张开 |
| 读心 | 点 NPC 名：clip-path 从左揭开手写批注 + 量筒液面下落 |
| Toast | 银盐显影（blur + brightness 复合曲线）+ 倒数条 |
| 确认框 | 倒数转盘从上方落下 + 旋转收敛 |

---

## 7. 无障碍与性能

**语义化**：`header/nav/main/aside/footer/section/article/dialog`；
放大机窗口 `role="dialog" aria-modal="false"`（非模态多窗口）；标签页 `role="tablist/tab/tabpanel"`；
量筒 `role="meter"`；正文 `aria-live="polite"`。

**唯一 ID**：全站 `p5-` 前缀语义命名（`p5-btn-sheet`、`p5-frame-composer`、`p5-proj-map`）。

**键盘**：`` ` ``/`Ctrl+K` 样张 · 方向键在样张 6 列网格上游走 · `Enter` 架进放大机 ·
`Esc` 逐层关闭（回合菜单 → 样张 → 抽屉 → 最上层投影）· `1/2/3` 三态 · `[`/`]` 两侧脊标 ·
输入框内 `/` 唤出指令、方向键选、`Tab` 补全。

**移动端触控**：`base.css` 里对 `button/[role=button]/[role=tab]/input/select/toggle/check/range`
统一 `min-height: var(--tap-min)`，图标按钮 44×44——**这是全局纪律，不靠逐个记得**。
唯一例外是正文里的可点人名（`.mind`），它属内容内交互，套 44px 会撕裂行高。

**性能**：零依赖零构建；面板首次打开才建 DOM（`contain: layout paint`）；
动画仅 `transform`/`opacity`；监听 `passive` + rAF 节流；图片 `lazy` + 失败回退程序化暗房场景；
粒子随 `visibilitychange` 暂停 + `ResizeObserver` 自适应；动效三档 + 特效开关，
`prefers-reduced-motion` 始终优先。

---

## 8. 交付形态

```
p5-demo/
  index.html
  styles/tokens.css      承袭 P3 色值与字体角色 + P5 布局层级（含 --tap-min）
  styles/base.css        reset / 排版 / 按钮 / 表单 / 焦点 / 移动端 44px 全局强制
  styles/darkroom.css    安全灯 / 显影量筒 / 相纸 / 银盐显影 / 晒片绳 / 干版 / 样张 / 场景底片
  styles/layout.css      横梁 / 显影槽 / 显影台三态 / 相片墙 / 工作台 / 移动端（控件预算约束）
  styles/panels.css      面板内组件（自 P4 移植，令牌与类名已映射到暗房体系）
  styles/enlarger.css    放大机投影 / 小样 Toast / 显影计时器 / 漏光 / 开场 / 快门 / 登录页
  styles/motion.css      进入编排 / 微交互 / reduced-motion 与动效档降级
  scripts/icons.js       112 枚手绘线性 SVG 图标（零 emoji）
  scripts/core.js        h() / 事件总线 / 持久化 / 标签页 / 控件工厂 / 焦点陷阱 / 涟漪
  scripts/data.js        暗房剧本 + 24 面板数据
  scripts/fx.js          浮尘 / 银盐显影打字机 / 视差 / 程序化暗房回退 / WebAudio 音效
  scripts/notify.js      相纸小样 / 显影计时器 / 漏光警示
  scripts/sheet.js       接触印相样张（唯一入口）+ 放大机多窗口
  scripts/stage.js       三态 / 相纸正文 / 晒片绳 / 读心 / 显影槽 / 相片墙 / 工作台
  scripts/panels-a|b|c.js  叙事 + 世界 + 系统族（自 P4 移植并改造）
  scripts/panels-d.js    P5 新增两格：剧场活动 / 记忆精炼器
  scripts/app.js         开场 / 快门 / 登录 / 装配 / 端切换 / 键盘
  checks.mjs             111 项验收（含控件预算闸门与最坏情形）
  audit.mjs              客观视觉审计（对比度 / 裁切 / 重叠 / 样张间距 / 调色板）
  shots.mjs              验收截图
  DESIGN.md  README.md
```

运行：**双击 `index.html`**（零构建、零依赖、`file://` 兼容）。
